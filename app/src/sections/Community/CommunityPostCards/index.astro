---
import './styles.css';

export interface CommunityPostCardsProps {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`community-postcards ${className}`}>
  <h2>Community PostCards Section</h2>
</div>
